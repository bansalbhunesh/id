# Test-only dependencies. Kept OUT of requirements.txt so the production
# container ships no test runner or HTTP test client. CI installs both files;
# the Docker image installs only requirements.txt.
pytest==9.1.1
httpx==0.28.1
